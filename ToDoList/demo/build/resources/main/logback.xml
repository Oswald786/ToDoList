<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%cyan(%d{HH:mm:ss.SSS}) %gray([%thread]) %highlight(%-5level) %magenta(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>
    <logger name="io.micronaut.context.env" level="DEBUG"/>

    <!-- Datasource/Hikari initialization -->
    <logger name="io.micronaut.configuration.jdbc.hikari" level="DEBUG"/>
    <logger name="com.zaxxer.hikari" level="DEBUG"/>

    <!-- Micronaut Data / JPA bootstrap -->
    <logger name="io.micronaut.data" level="DEBUG"/>
    <logger name="io.micronaut.configuration.hibernate.jpa" level="DEBUG"/>

    <!-- Hibernate core + SQL -->
    <logger name="org.hibernate" level="INFO"/>
    <logger name="org.hibernate.engine.jdbc.env.internal" level="DEBUG"/>
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.orm.jdbc.bind" level="TRACE"/>
    <logger name="org.hibernate.type.descriptor.sql" level="TRACE"/>


</configuration>
